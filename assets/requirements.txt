# Research Supercharged — Build (SDK) rail dependencies
# Current Microsoft Foundry agents API (NOT the classic Assistants / azure-ai-agents).
# Verified against azure-ai-projects 2.2.0 / openai 2.43.0 (Python 3.10+).

azure-ai-projects>=2.2.0      # Foundry agents API (create_version + PromptAgentDefinition + tools) and cloud evaluation (bonus lab)
openai>=2.8.0                 # OpenAI-compatible client (Responses API) returned by get_openai_client()
azure-identity>=1.15.0        # DefaultAzureCredential (run `az login` first)
python-dotenv>=1.0.0          # optional: load a local .env

# --- optional, only if a participant wants code-first orchestration in the stretch lab ---
# agent-framework>=1.0.0       # Microsoft Agent Framework (FoundryChatClient + workflows)
